public interface Matrix<T extends Number> {
   // void multiplication(Matrix<T> matrix) throws IllegalArgumentException;
   // void resultant(Matrix<T> matrix) throws IllegalArgumentException;
    void transposition();
}
